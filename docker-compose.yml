services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: dreamfut
      POSTGRES_USER: dreamfut
      POSTGRES_PASSWORD: dreamfut
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U dreamfut -d dreamfut']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres-data:
